// Loader.jsx
import React from 'react';
import './Loader.css'; // Importing the CSS for the loader styling

const Loader = () => (
  <div className="loader-container">
    <div className="spinner"></div>
  </div>
);

export default Loader;
